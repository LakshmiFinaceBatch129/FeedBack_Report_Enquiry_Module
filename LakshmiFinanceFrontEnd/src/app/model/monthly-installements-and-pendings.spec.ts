import { MonthlyInstallementsAndPendings } from './monthly-installements-and-pendings';

describe('MonthlyInstallementsAndPendings', () => {
  it('should create an instance', () => {
    expect(new MonthlyInstallementsAndPendings()).toBeTruthy();
  });
});
